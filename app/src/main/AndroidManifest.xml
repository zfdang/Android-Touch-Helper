<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Package visibility (API 30+): the service needs the list of launchable apps to build its
        working list / whitelist, and the list of input methods to ignore their windows. Both are
        resolved through these queries, so QUERY_ALL_PACKAGES is not needed.
        Overlays are TYPE_ACCESSIBILITY_OVERLAY windows of the accessibility service, which need
        no SYSTEM_ALERT_WINDOW; the battery page is opened via
        ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS, which needs no permission either.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
        <intent>
            <action android:name="android.view.InputMethod" />
        </intent>
    </queries>

    <application
        android:name="com.zfdang.TouchHelperApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_touch_helper_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_touch_helper_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".TouchHelperService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:enabled="true"
            android:exported="true"
            android:label="@string/touch_helper_service_label">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>


</manifest>